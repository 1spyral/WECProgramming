# WECProgramming

Requirements:
pandas (and associated libraries, etc)

Servers file:

/input/server.csv

Tasks file:

/input/tasks.csv


How to run the code:

First, install requirements:

pip install pandas

Run the file:

python main.py